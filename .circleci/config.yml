jobs:
  build-and-push:
    executor: docker/docker
    steps:
      - checkout
      - docker/check
      - docker/build:
          image: nicolaspascual/circleci:$CIRCLE_SHA1
orbs:
  docker: circleci/docker@1.0.0
version: 2.1
workflows:
  commit:
    jobs:
      - build-and-push:
          context: DOCKER