jobs:
  build-and-push:
    executor: docker/docker
    steps:
      - setup_remote_docker
      - checkout
      - docker/check
      - docker/build:
          image: ${DOCKER_LOGIN}/circleci
orbs:
  docker: circleci/docker@1.0.0
version: 2.1
workflows:
  commit:
    jobs:
      - build-and-push:
          context: DOCKER