jobs:
  build-and-push:
    executor: docker/docker
    steps:
      - checkout
      - docker/check
      - docker/build:
          image: nicolaspascual/circleci:$CIRCLE_SHA1
orbs:
  docker: circleci/docker@x.y.z
version: 2.1
workflows:
  commit:
    jobs:
      - build-and-push